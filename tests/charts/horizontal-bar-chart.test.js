var HorizontalBarChart = require('charts/horizontal-bar-chart');

describe('horizontal bar chart', function() {
  it ('exists', function() {
    if (HorizontalBarChart) {
      return true;
    }
  });
});
